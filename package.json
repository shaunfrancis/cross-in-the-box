{
  "name": "cross-in-the-box",
  "version": "5.0.0",
  "scripts": {
    "build": "node src/generate-script.js; node src/generate-imports.js; npx parcel build src/style.css --dist-dir compiled --no-source-maps"
  },
  "devDependencies": {
    "parcel": "^2.15.4"
  }
}
